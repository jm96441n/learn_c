#include <stdio.h>

/* this is a comment. */

int main(int argc, char *argv[])
{
  int distance = 10;
  char time[5] = "10:15";
 
  // this is also a comment
  printf("You are %d miles away \n", distance);
  printf("The time now is %s in the morning \n", time);
 
  return 0;
}
